
# Image Compressor Requirements
# Core image processing library
Pillow>=10.0.0

# For building executable (optional - only needed for development/building)
pyinstaller>=5.13.0

# Standard library modules used (no installation required):
# - tkinter (included with Python)
# - threading (included with Python) 
# - pathlib (included with Python)
# - os (included with Python)
# - time (included with Python)